pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Compilar el programa Java
                sh 'javac src/Main.java'
            }
        }
        stage('Test') {
            steps {
                 script {
                    def mensaje= sh(script:'java -cp src Main',returnStdout: true)
                    if (mensaje == "hola mundo,todo bien") {
                        echo "El mensaje es correcto"
                    } else {
                        error "El mensaje es incorrecto"
                    }
               
            }
        }
    }
}
}
